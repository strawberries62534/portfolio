{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cd dist && cp index.html 404.html && cd .. && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-i18n": "^11.0.1",
    "vue-router": "^4.0.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.1"
  }
}
